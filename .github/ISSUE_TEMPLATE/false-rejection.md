---
name: False rejection
about: Tenet rejects a declaration that Lean accepts
labels: bug
---

**The failure message** (paste the `FAIL ...` block from `tenet check`)

**Smallest export that shows it** (attach the `.ndjson`, or the Lean source and the
`lean4export` command that produced it)

**Lean version and Tenet version**
